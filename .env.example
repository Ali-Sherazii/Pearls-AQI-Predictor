# Hopsworks (free tier: https://app.hopsworks.ai) - feature store + model registry.
# Leave both unset to use the local parquet/joblib fallback under data/ and
# models/ instead (see src/feature_store/store.py, src/models/registry.py).
HOPSWORKS_API_KEY=
HOPSWORKS_PROJECT=
